# CloudEnergy

Global hybrid renewable-energy, battery-storage and digital capacity-reservation platform.

## Development

```bash
npm install
npm run dev
```

Production builds are generated with `npm run build` and deployed from the `main` branch through Vercel.
